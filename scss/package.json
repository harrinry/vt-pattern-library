{
  "name": "vt-scss",
  "version": "1.0.0",
  "description": "",
  "author": "Web Squad",
  "license": "ISC",
  "main": "dist/styles.min.css",
  "publishConfig": {
    "registry": "https://artifactory.aquent.io/artifactory/api/npm/aquent-npm/"
  },
  "scripts": {
    "build": "rollup -c --environment TARGET:development && rollup -c --environment TARGET:production",
    "build:dev": "rollup -c --environment TARGET:development",
    "watch": "rollup -c --environment TARGET:development --watch --dir tmp"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aquent/vt-pattern-library/scss"
  },
  "keywords": [
    "scss",
    "css",
    "styling"
  ],
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "cssnano": "^5.0.7",
    "modern-css-reset": "^1.4.0",
    "node-sass": "^6.0.1",
    "postcss": "^8.1.0",
    "postcss-cli": "^8.3.1",
    "postcss-import": "^14.0.2",
    "rollup": "^2.56.0",
    "rollup-plugin-scss": "^3.0.0"
  }
}
